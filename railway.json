{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm build"
  },
  "deploy": {
    "startCommand": "cd .medusa/server && pnpm install && pnpm predeploy && pnpm start"
  }
}
