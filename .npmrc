; @medusajs packuments are huge (15MB+ for admin-sdk) and trip Cloudflare's
; rate limiter in front of registry.npmjs.org (error 1015). This mirror serves
; byte-identical tarballs; the lockfile's integrity hashes still verify them.
@medusajs:registry=https://registry.npmmirror.com
